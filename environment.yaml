name: panmixer
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - bcftools
  - plink
  - pip
  - pip:
    - ortools
    - numpy
    - pandas
    - scipy
    - scikit-learn
    - matplotlib
